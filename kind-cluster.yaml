kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # Port pour l'application J2EE (évite 8080 et 8081)
      - containerPort: 30080
        hostPort: 8090
        protocol: TCP
      # Port HTTPS (optionnel)
      - containerPort: 30443
        hostPort: 8443
        protocol: TCP